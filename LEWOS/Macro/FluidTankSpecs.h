#ifndef _TANK_SPECS
#define _TANK_SPECS

#define num_local_domains 		4	//the number of local domains (i.e number of cards)
#define global_domain_width		1	//the width of the grid (number of actuators along horizontal direction)
#define global_domain_height	16	//the height of the grid (number of actuators along vertical direction)
#define local_domain_width 		1	//the width of each local domain, where a local domain consists of actuators controlled by a single card
#define local_domain_height		4	//the height of each local domain, where a local domain consists of actuators controlled by a single card

#endif